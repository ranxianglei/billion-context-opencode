{
  "name": "billion-context-opencode-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace billion-context-opencode && npm run build --workspace billion-context-opencode-v2",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace billion-context-opencode"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  },
  "license": "MIT"
}
